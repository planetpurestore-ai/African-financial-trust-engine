# PostgreSQL connection string. Use a managed PostgreSQL provider in production.
DATABASE_URL=postgresql+psycopg://USER:PASSWORD@HOST:5432/trust_engine

# Long random secret used to hash API keys at rest.
API_KEY_PEPPER=replace-with-a-long-random-secret
